# BUGFIX16: MMC3 scanline timing — A12 notification phase alignment

**Date**: 2026-02-22
**Baseline**: 159 PASS / 15 FAIL → **161 PASS / 13 FAIL** (+2 PASS)

## Problem

- `mmc3_test/4-scanline_timing` — FAIL #3: "Scanline 0 IRQ should occur sooner when $2000=$08"
- `mmc3_test_2/4-scanline_timing` — Same as above

## Root Cause

The A12 notification timing in the PPU rendering pipeline was too late. On real NES hardware the A12 rising edge appears during the address setup phase, but the emulator was notifying the mapper at a later phase.

Test calibration values:
- `scanline_0_08 = 6976` (BG at $0000, sprites at $1000)
- `scanline_0_10 = 6720` (BG at $1000, sprites at $0000)
- Difference = 256 PPU cycles = sprite fetch start (cx=260) − BG CHR fetch start (cx=4)

When $2000=$08, the first A12 rising edge comes from sprite CHR fetch. Per-scanline sprite fetches start at dot 257, in groups of 8 cycles. Phase 3 (cx=260) is the correct timing for the sprite CHR address to be on the bus.

## Fix (PPU.cs `ppu_rendering_tick()`)

### BG tile fetch (dots 0-255, 320-335)
- Phase 0: NT address → `NotifyMapperA12(ioaddr)` (A12=0)
- Phase 4: CHR low address → `NotifyMapperA12(ioaddr)` (A12=BG table bit)

### Sprite fetch (dots 257-319)
- Phase 0: garbage NT → `NotifyMapperA12(0x2000)` (A12=0)
- Phase 3: sprite CHR → `NotifyMapperA12(SpPatternTableAddr)` (A12=sprite table bit)

### Garbage NT (dots 336-339)
- Dot 336: `NotifyMapperA12(0x2000)` (A12=0)

Key change: sprite A12=1 notification moved from phase 4 (cx=261) to phase 3 (cx=260), causing scanline 0 IRQ to fire 1 PPU cycle earlier in $2000=$08 mode.

## Verification

```
=== RESULTS: 161 PASS / 13 FAIL / 174 TOTAL ===

All MMC3 tests passing:
- mmc3_irq_tests: 6/6 PASS
- mmc3_test: 6/6 PASS
- mmc3_test_2: 6/6 PASS
```

## Incidental Improvement: run_tests_report.sh Refactor

- Added `--json`, `--screenshots`, `--no-build` parameters
- Fixed merged test max-wait 60→120 (consistent with run_tests.sh)
- Synced run_tests.sh to include mmc3 tests (+18 tests → 174 TOTAL)
