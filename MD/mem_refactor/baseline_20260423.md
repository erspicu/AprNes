# MEM Refactor — Baseline

- **Date**: 2026-04-23
- **Branch**: `feature/mem-refactor` @ (just branched from master 9ae699f)
- **Benchmark**: `AprNes/bin/Debug/benchmark_baseline.bat`
- **Config**: NTSC / 1x (256×240) / Audio Mode 0 (Pure Digital), no filters, no analog, no CRT
- **ROM**: `ny2011.nes`
- **Duration**: 20s per run, 3-run protocol
- **Hardware**: AMD Ryzen 7 3700X 8-core / 32 GB RAM / RTX 4080

## Results

| Run | FPS | Note |
| --- | ---:| --- |
| JIT warmup | 105.05 | discarded (Tier-0 + PGO collection) |
| Run 2 | **120.16** | measured |
| Run 3 | **120.35** | measured |

**Baseline = (120.16 + 120.35) / 2 = 120.26 FPS**

Realtime reference: 60.10 FPS → baseline = **2.00× realtime**.

## Notes

- Cooldown between runs failed (`timeout /t` shadowed by Git Bash's `timeout`), but Run 2 and Run 3 differ by 0.19 FPS — thermal throttling negligible over 20s runs.
- Debug build. Release build expected higher but tier comparison relative to this number is what matters.

## Next

After each MEM-refactor change, re-run `benchmark_baseline.bat` and record delta here. Target: ≥ baseline FPS, no regression in blargg 184/184 + AccuracyCoin 138/138.

---

## Run 1 — 8-Page Table Refactor (2026-04-23)

Replaced the 65536-entry `mem_read_fun` / `mem_write_fun` tables (1 MB total) with an 8-entry `mem_read_page` / `mem_write_page` table indexed by `addr >> 13`. Page 2 ($4000-$5FFF) uses an internal sub-dispatcher for the mixed APU / OpenBus / ExpansionROM ranges. FDS migrated from per-address overrides (~40 lines) to per-page overrides (~16 lines) plus a dedicated `fds_Read_Page2` / `fds_Write_Page2` dispatcher.

### Results

| Run | Before | After | Δ |
| --- | ---:| ---:| ---:|
| JIT warmup | 105.05 | 121.53 | +16.5% |
| Run 2 | 120.16 | 120.25 | +0.07% |
| Run 3 | 120.35 | 122.50 | +1.79% |
| **Average** | **120.26** | **121.38** | **+0.93%** |

### Regression Check

- blargg: **184/184 PASS** (48.5s)
- AccuracyCoin: deferred to user's own run

### Interpretation

+0.93% is within expected range (0-2%, previously predicted "likely near 0"). Run-to-run variance (Run 2 vs Run 3 diff of 2.25 FPS) is larger than the delta itself — the refactor is at worst neutral and probably marginally beneficial. Actual motivation for this change was architectural cleanliness, not performance.

### Other Notes

- Memory footprint: reduced by ~1 MB (both tables went from 65536 × 8 bytes × 2 to 8 × 8 bytes × 2)
- The 8-slot table (64 bytes) fits in one cache line — guaranteed L1-resident
- FDS reset-vector read (`mem_read_fun[0xFFFC/0xFFFD]`) now uses `mem_read_page[7](...)` directly
